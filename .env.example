# StaffyAPI base URL — host of the /api/salus-leads endpoint that handles
# the PilotModal and contact form submissions. Defaults to https://api.staffy.com
# at build time if unset. Override for staging or local backend testing.
GATSBY_STAFFY_API_BASE=https://api.staffy.com
